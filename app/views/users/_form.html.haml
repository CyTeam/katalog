= semantic_form_for @user do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :username
    = f.input :email
    = f.input :password, :hint => !f.object.new_record?, :input_html => {:autocomplete => "off"}
    = f.input :password_confirmation
    -# f.input :current_password, :required => true unless can?(:manage, User)
    = f.input :role_texts, :as => :radio, :collection => Ability.roles_for_collection, :include_blank => false, :required => true if can?(:manage, Role)

  = f.buttons do
    = f.commit_button
