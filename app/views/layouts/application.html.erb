<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <title>Katalog</title>

  <%= stylesheet_link_tag 'doku-zug.ch/typolight', 'doku-zug.ch/basic', 'doku-zug.ch/content', 'doku-zug.ch/layout', 'doku-zug.ch/navigation', 'doku-zug.ch/welcome', 'doku-zug.ch/application' %>
  <%= stylesheet_link_tag 'doku-zug.ch/print', :media => 'print' %>
  <%= stylesheet_link_tag 'katalog', :media => 'all' %>
  <%= stylesheet_link_tag 'katalog_print', :media => 'print' %>
  <%= stylesheet_link_tag 'katalog_internal' if user_signed_in? %>
  <%= stylesheet_link_tag 'scaffold' %>
  <%= stylesheet_link_tag 'formtastic', 'formtastic_changes' %>
  <%= stylesheet_link_tag 'pagination' %>

  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>

<body id="top">
  <div id="wrapper">
    <div id="header">
      <div class="inside">
        <%= render user_signed_in? ? 'layouts/internal_header' : 'layouts/public_header' %>
      </div>
    </div>

    <div id="container">
      <div id="left">
        <div class="inside">
          <div class="mod_navigation block" id="mainmenu">
            <a href="#skipNavigation3" class="invisible">Navigation überspringen</a>
            <ul class="level_1">
              <%= navigation_item("Dossier auflisten", dossiers_path) %>
              <%= navigation_item("Dossier suchen", search_dossiers_path) %>
              <%= navigation_item("Dossier anlegen", new_dossier_path) %>
              <%= navigation_item(t('controllers.index.keyword'), index_keyword_path) %>
              <% if user_signed_in? %>
                <%= navigation_item("Abmelden", destroy_user_session_path) %>
              <% else %>
                <%= navigation_item("Anmelden", new_user_session_path) %>
              <% end %>
            </ul>
            <a name="skipNavigation3" id="skipNavigation3" class="invisible">&nbsp;</a>
          </div>
        </div>
      </div>

      <div id="right">
        <div class="inside">
          <div class="mod_newslist block">
            <h1>Blättern</h1>

            <div class="layout_full block odd">
              <%= render 'layouts/topic_index' %>
            </div>

            <h1>Stöbern</h1>

            <div class="layout_full block last odd">
              <%= render 'layouts/tag_cloud' %>
            </div>
          </div>
        </div>
      </div>

      <div id="main">
        <div class="inside">
          <div class="mod_newslist block">
          <%= yield %>
          </div>
        </div>
         
        <div id="clear"></div>
      </div>
    </div>

    <div id="footer">
      <div class="inside">

        <%= render 'layouts/public_footer' unless user_signed_in? %>

        <div class="clear"></div> 
      </div>
    </div>
  </div>
</body>
</html>
