= render 'search_form'
%h1= t('controllers.index.keyword')
= paginated_section :show_always => true do
  .ce_table.block
    %table
      - cache :key_words_header do
        %thead
          %tr
            %th= t_attr('keyword', Dossier)
      %tbody
        = render @keywords.map{|keyword| keyword.becomes(Keyword)}
