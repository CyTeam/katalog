<h1><%= t('controllers.index.keyword') %></h1>

<%= render 'search_form' %>

<%= paginated_section do %>
<div class="ce_table block">
<table>
  <tr>
    <th><%= t_attr('keyword', Dossier) %></th>
  </tr>
  <%= render @keywords.map{|keyword| keyword.becomes(Keyword)} %>
</table>
</div>
<% end %>
