<%= semantic_form_for(:keyword, @keyword, :url => dossier_keywords_path(@dossier), :remote => true, :html => {:id => 'new-keyword'}) do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
  <% end %>

  <%= f.buttons do %>
    <%= f.commit_button %>
  <% end %>
<% end %>
