= render 'search_title'
.index
  #dossiers.ce_table.block.landscape-table
    %table
      %tr.header
        - @columns.each do |attr|
          %th{:class => attr.to_s}= t_attr(attr.to_s)
        
      = render :partial => @dossiers, :locals => {:report => true}
