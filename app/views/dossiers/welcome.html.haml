%ul#welcome-overview
  %li
    = link_to dossiers_path do
      = image_tag "/images/dossiers/1.png"
      %h1= t('katalog.overview')
      %h2= t('katalog.overview_text')
  - @groups.each do |group|
    %li
      = image_tag "/images/dossiers/#{group.signature}.png", :alt => group.to_s
      %h1
        = link_to dossier_path(group) do
          = group
      %h2= t(:count, :scope => :katalog) + ": " + number_with_delimiter(group.document_count)
      -# group.random_children.each do |dossier|
        =# link_to dossier, dossier_path(dossier), :class => 'dossiers'

