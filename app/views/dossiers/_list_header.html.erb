<% cache :dossier_list_header do %>
  <tr class="header">
    <th class="signature"><%= t_attr('signature', Dossier) %></th>
    <th class="title"><%= t_attr('title', Dossier) %></th>
    <th class="document_count" style="text-align: right"><%= t_attr('document_count', Dossier) %></th>
    <th class="action-links"></th>
  </tr>
<% end %>
