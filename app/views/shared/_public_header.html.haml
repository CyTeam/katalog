#header
  .inside
    #logo= link_to image_tag("doku-zug.ch/logo.gif", :alt => t('katalog.shared.logo.alt')), "http://www.doku-zug.ch", :title => t('katalog.shared.logo.title')
    .mod_breadcrumb.block= render 'shared/breadcrumb_navigation'
