#page_actions.contextual
  -#= contextual_link('pdf', url_for(params.merge({:format => :pdf, :per_page => 'all'})), :title => t('tooltips.dossiers.show_pdf'))
  -#- if user_signed_in?
  -#  = contextual_link('overview', report_dossiers_path(:format => :pdf, :report_name => '5-year'), :title => t('tooltips.dossiers.show_themeoverview'))
  %ul#report_actions
    %li
      %a{:href => '#'}
        Reports
      %ul
        - Report.all.each do |report|
          %li
            = report.title

