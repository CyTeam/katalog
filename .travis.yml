before_script:
  - sudo apt-get install -y aspell libaspell-dev aspell-en aspell-de && bundle install
  - "mysql -e 'create database katalog_test;'"
  - DB=mysql bundle exec rake katalog:setup
  - DB=mysql bundle exec rake db:create db:schema:load

script: "bundle exec rake test"

# whitelist
branches:
  only:
    - master
    - stable

# IRC notification
notifications:
  irc: "irc.freenode.org#cyt"
